import { ContainerRuntime, ExecResult } from "../../types";
import Dockerode from "dockerode";
export declare const execContainer: (dockerode: Dockerode, containerRuntime: ContainerRuntime, container: Dockerode.Container, command: string[], shouldLog?: boolean) => Promise<ExecResult>;
