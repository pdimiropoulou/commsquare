import Dockerode from "dockerode";
import { Readable } from "stream";
export type ContainerLogsOptions = {
    since?: Date;
};
export declare const containerLogs: (container: Dockerode.Container, options?: ContainerLogsOptions) => Promise<Readable>;
