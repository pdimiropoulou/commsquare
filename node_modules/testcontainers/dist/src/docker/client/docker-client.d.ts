/// <reference types="node" />
import Dockerode from "dockerode";
import { HostIps } from "../lookup-host-ips";
import { SystemInfo } from "../../system-info";
import { ContainerRuntime } from "../types";
export type DockerClient = DockerClientStrategyResult & {
    host: string;
    containerRuntime: ContainerRuntime;
    hostIps: HostIps;
    info: SystemInfo;
};
export type DockerClientStrategyResult = {
    uri: string;
    dockerode: Dockerode;
    composeEnvironment: NodeJS.ProcessEnv;
    allowUserOverrides: boolean;
};
export declare function getDockerClient(): Promise<DockerClient>;
