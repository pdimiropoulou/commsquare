/// <reference types="node" />
import { DockerClientStrategyResult } from "../docker-client";
import { DockerClientStrategy } from "./docker-client-strategy";
export declare class RootlessUnixSocketStrategy implements DockerClientStrategy {
    private readonly platform;
    private readonly env;
    private applicable;
    private socketPath;
    constructor(platform?: NodeJS.Platform, env?: NodeJS.ProcessEnv);
    init(): Promise<void>;
    private getSocketPathFromEnv;
    private getSocketPathFromHomeRunDir;
    private getSocketPathFromHomeDesktopDir;
    private getSocketPathFromRunDir;
    getDockerClient(): Promise<DockerClientStrategyResult>;
    isApplicable(): boolean;
    getName(): string;
}
