import { DockerImageName } from "./docker-image-name";
import { BuildArgs } from "./docker/types";
export declare const getDockerfileImages: (dockerfile: string, buildArgs: BuildArgs) => Promise<DockerImageName[]>;
