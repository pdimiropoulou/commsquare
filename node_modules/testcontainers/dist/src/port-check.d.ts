import Dockerode from "dockerode";
import { ContainerRuntime } from "./docker/types";
export interface PortCheck {
    isBound(port: number): Promise<boolean>;
}
export declare class HostPortCheck implements PortCheck {
    private readonly host;
    constructor(host: string);
    isBound(port: number): Promise<boolean>;
}
export declare class InternalPortCheck implements PortCheck {
    private readonly dockerode;
    private readonly containerRuntime;
    private readonly container;
    private isDistroless;
    private commandOutputs;
    constructor(dockerode: Dockerode, containerRuntime: ContainerRuntime, container: Dockerode.Container);
    isBound(port: number): Promise<boolean>;
    private commandOutputsKey;
}
