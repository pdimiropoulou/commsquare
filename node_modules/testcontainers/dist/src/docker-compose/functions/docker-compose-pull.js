"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.dockerComposePull = void 0;
const logger_1 = require("../../logger");
const docker_compose_1 = require("docker-compose");
const default_docker_compose_options_1 = require("../default-docker-compose-options");
const dockerComposePull = (options, services) => __awaiter(void 0, void 0, void 0, function* () {
    try {
        if (services) {
            logger_1.log.info(`Pulling DockerCompose environment images "${services.join('", "')}"...`);
            yield (0, docker_compose_1.pullMany)(services, yield (0, default_docker_compose_options_1.defaultDockerComposeOptions)(Object.assign(Object.assign({}, options), { logger: logger_1.pullLog })));
        }
        else {
            logger_1.log.info(`Pulling DockerCompose environment images...`);
            yield (0, docker_compose_1.pullAll)(yield (0, default_docker_compose_options_1.defaultDockerComposeOptions)(Object.assign(Object.assign({}, options), { logger: logger_1.pullLog })));
        }
        logger_1.log.info(`Pulled DockerCompose environment`);
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
    }
    catch (err) {
        const errorMessage = err.err || err.message || err || "";
        logger_1.log.error(`Failed to pull DockerCompose environment images: ${errorMessage.trim()}`);
        throw new Error(errorMessage);
    }
});
exports.dockerComposePull = dockerComposePull;
